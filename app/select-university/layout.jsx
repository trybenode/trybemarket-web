export const metadata = {
  title: "Choose Your Campus",
  description: "Choose your university to complete your TrybeMarket profile.",
  robots: { index: false, follow: false },
};

export default function Layout({ children }) {
  return children;
}
